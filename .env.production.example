# Variables de entorno para producción
# Este archivo es una referencia de ejemplo para los desarrolladores
# IMPORTANTE: El .env real se genera en GitHub Actions desde el secret DOT_ENV

# URL base de la API en producción
# Se usa tanto en:
# - npm run build (compilación en GitHub Actions)
# - docker-compose (runtime en VPS)
VITE_BASE_API_URL=https://api.tudominio.com

# URL base del servidor (Strapi u otro backend)
# Usado en runtime en el servidor Node SSR
VITE_BASE_SERVER_URL=https://api.tudominio.com

# Configurar estas variables en GitHub Secrets como DOT_ENV
# Ejemplo del contenido del secret DOT_ENV:
# VITE_BASE_API_URL=https://api.produccion.com
# VITE_BASE_SERVER_URL=https://api.produccion.com
